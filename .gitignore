# Dependencies
node_modules

# Build output
dist
dist-ssr
out
release
*.tsbuildinfo

# Environment (secrets). NUNCA commitear estos archivos.
.env
.env.*
!.env.example

# Supabase local/temp (refs de proyectos, tokens locales)
.supabase
supabase/.temp

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claves de firma de QZ Tray. NUNCA commitear (se generan por instalación
# vía "QZ Tray > Advanced > Site Manager", o se distribuyen como override.crt).
auth/*
!auth/README.md

# OS
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea
